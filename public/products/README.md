# Product tile images

Files here are copied from `D:\SilkPanda\Artifacts\Products` (see `npm run artifacts:sync`).

Referenced in the app as `/products/saree-01.jpg`, etc. — see `lib/products/artifact-images.ts`.
